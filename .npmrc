# Disable the 24-hour release-age gate so Cloudflare Pages builds don't fail
# when the lockfile contains packages published within the last 24 hours.
# Safe for a single-developer project with a trusted lockfile.
minimum-release-age=0
