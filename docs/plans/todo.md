- allow users to submit suggestions and route them directly to github issues FOR THIS REPO
- formatting on daily reflections is kind of garbage - can we just pull from the AAWS site on that particular day and fallback to the text we use for concordance context
- if I search for "Havent got" I miss searches for "Haven't got" - is there a relatively easy workaround for this?
- Can we use AI text processing to build a static 'sentiment' or 'logical' grouping for some of these terms and ideas? I don't want to imbed AI but using it to generate a different type of index might be useful. Or maybe a fuzzy search? If I search for God, I don't get Spirit of the Universe or Creator, and vice-versa. Could maybe create a 'synonym' dictionary for God that includes Higher Power, Yaweh, etc.
- Set the icon for PWA to be the same as the 'bt' icon used on mobile - Currently it is just a gray square with the letter P in white.
- 'FILTER SOURCES' little circle/badge is hidden when the source is selected (true on both light and dark themes)
- The word 'tornado' or 'Tornado' doesn't return results and it should, it's from Into Action p.82
- We don't need both basic texts. I think the 2nd edition would be fine. Argue with me on this but it seems like unnecessary overhead.


